@model IEnumerable<Gestion_Prestamos.Models.Rol>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.rol_descripcion)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.rol_estado)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.rol_descripcion)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.rol_estado)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.id_rol">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.id_rol">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.id_rol">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
